<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="myDialogTheme" parent="android:Theme.Dialog">
        <!-- 除去title -->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 除去背景色 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:background">@color/transparent</item>
    </style>
    <style name="Theme.AppStartLoad" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@mipmap/splash_bg</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="DialogDown">
        <item name="android:windowAnimationStyle">@style/DialogAnimDown</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 除去title -->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 除去背景色 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:background">@color/transparent</item>
    </style>
    <style name="DialogAnimDown">
        <item name="android:windowExitAnimation">@anim/dialog_share_in</item>
        <item name="android:windowEnterAnimation">@anim/dialog_share_out</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
